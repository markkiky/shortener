<!DOCTYPE html>
<html>

<head>
    <title>Your short links statistics</title>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding: 20px;
        }

        h1 {
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }

        th {
            background-color: #f8f9fa;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        p {
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Here is a report of your links performance</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>URL</th>
                    <th>Short Code</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% @links.each do |link| %>
                <tr>
                    <td><%= link.url %></td>
                    <td><%= link.short_code %></td>
                    <td><%= link.clicks %></td>
                </tr>
                <% end %>
            </tbody>
        </table>

        <p>Thanks for making us your shortening app of choice and have a great day!</p>
    </div>
    <!-- Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
